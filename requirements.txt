# sudo apt update
# sudo apt install python3-venv

# cd ./bind_to_selectel_api_dns
# python3 -m venv .venv
# source .venv/bin/activate

# pip install -r requirements.txt
# pip install -I --user idna
requests
